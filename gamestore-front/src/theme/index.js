import { createTheme } from '@mui/material/styles';

export const themeSite = createTheme({
  palette: {
    primary: {
      main: '#181818',
    },
    secondary: {
      main: '#f15a28',
    },
    }
});